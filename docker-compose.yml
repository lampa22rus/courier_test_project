version: '3'

services:
  postgres:
    image: postgres:12.1
    ports: 
      - "127.0.0.1:5432:5432"
    restart: always
    environment:
      - POSTGRES_USER=courier
      - POSTGRES_PASSWORD=courier
      - POSTGRES_DB=courier
    volumes:
      - ./.data/postgresql:/var/lib/postgresql/data